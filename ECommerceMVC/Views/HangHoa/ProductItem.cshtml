@model IEnumerable<ECommerceMVC.ViewModels.HangHoaVM>
<h3>có @Model.Count() sản phẩm</h3>
@foreach (var item in Model)
{
    <div class="col-md-6 col-lg-6 col-xl-4">
        <div class="rounded position-relative fruite-item">
            <a asp-action="ProductDetail" asp-controller="HangHoa" asp-route-id="@item.Id">
            <div class="fruite-img">
                <img src="~/Hinh/HangHoa/@item.Hinh" class="img-fluid w-100 rounded-top" alt="@item.TenHH">
            </div>
            </a>
            <div class="text-white bg-secondary px-3 py-1 rounded position-absolute" style="top: 10px; left: 10px;">@item.TenLoai</div>
            <div class="p-4 border border-secondary border-top-0 rounded-bottom">
                <a asp-action="ProductDetail" asp-controller="HangHoa" asp-route-id="@item.Id">
                <h4>@item.TenHH</h4>
                </a>
                <p>@item.MoTaNgan</p>
                <div class="d-flex justify-content-between flex-lg-wrap">
                    <p class="text-dark fs-5 fw-bold mb-0">$4.99 / kg</p>
                    <a href="#" class="btn border border-secondary rounded-pill px-3 text-primary"><i class="fa fa-shopping-bag me-2 text-primary"></i> add to card</a>
                </div>
            </div>
        </div>
    </div>
}